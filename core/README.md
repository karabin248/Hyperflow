# Core

This directory is **not** a primary documentation entrypoint.

For active documentation, start with:
- `README.md`
- `docs/README.md`

Use `core/` only for canonical runtime contracts and freeze/spec files already referenced from those entrypoints.
